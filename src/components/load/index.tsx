import {ActivityIndicator} from 'react-native';
import colors from '../../themes/colors';

const Load = () => {
  return <ActivityIndicator size="small" color={colors.primary.primary950}/>;
};

export default Load;
